<%= tag.div(id: dom_id(standup_meeting_group), class: "p-2 border shadow-lg rounded-lg max-w-prose mx-auto flex-1") do %>
  <div class="card-body">
    <h2 class="card-title font-bold text-2xl"><%= link_to standup_meeting_group.name, standup_meeting_group_path(standup_meeting_group.id) %></h2>
    <div class="card-actions">
      <button class="btn btn-sm">Check In</button>
      <button class="btn btn-sm">Dismiss</button>test
      <%#= link_to 'Leave', standup_meeting_group_join_path(@standup_meeting_group, 2 ), data: { turbo_method: :delete }, class: 'btn btn-danger' %>

      <%= render StandupMeetingGroup::LeaveButtonComponent.new(standup_meeting_group: standup_meeting_group, current_user: user, standup_meeting_group_user:@standup_meeting_group_user ) %>
      <%#= @standup_meeting_group_user = standup_meeting_group.standup_meeting_groups_users.where(user_id: user.id)  %>
      <%# standup_meeting_group_user is a variable that represents the join record that associates the user with standup_meeting_group.  to find this standup_meeting_group_user we can 
      do this: standup_meeting_group_user = standup_meeting_group.standup_meeting_group_users.find_by(user: user) this will find the join record that associates the user with the standup_meeting_group %>
  
    </div>
  </div>
<% end %>
